# Συνεισφορά

Το υλικό γράφεται σε Markdown και από εκεί παράγονται τόσο η ιστοσελίδα
(GitHub Pages) όσο και τα PDF. Πηγή αλήθειας είναι πάντα το
`labs/labNN/README.md` — τα PDF δεν επεξεργαζόμαστε ποτέ με το χέρι.

## Προϋποθέσεις

1. `make`
2. `docker`

Δεν χρειάζεται τοπική εγκατάσταση pandoc ή LaTeX — και τα τρία στάδια του build
τρέχουν μέσα σε containers.

## Build

```sh
make                          # όλα τα PDF των εργαστηρίων + build/all.pdf
make build/lab05.pdf          # ένα μόνο εργαστήριο (γρήγορος κύκλος δοκιμών)
make labs/lab05/README-out.md # μόνο το στάδιο mermaid → PNG
```

Τα παραγόμενα PDF βρίσκονται στον κατάλογο `build/`.

Τα ενδιάμεσα αρχεία `README-out.md` και `README-out-*.png`, όπως και ο κατάλογος
`build/`, είναι στο `.gitignore` και δεν πρέπει να μπαίνουν σε commit.

## Πώς δουλεύει το build

Για κάθε εργαστήριο εκτελούνται δύο στάδια:

1. **`minlag/mermaid-cli`** — μετατρέπει τα μπλοκ ` ```mermaid ` σε PNG και παράγει
   το `README-out.md`.
2. **`ghcr.io/ethan42/pandoctex`** — `pandoc` + `xelatex`, με το κοινό προοίμιο
   `labs/header.tex` (χρώματα, κεφαλίδες, λογότυπα).

Το συνολικό `build/all.pdf` παράγεται χωριστά: το `tools/build-all.py` ενώνει τα
έντεκα `README-out.md` σε ένα αρχείο και ακολουθεί **μία** μεταγλώττιση με pandoc,
μαζί με το `labs/cover.tex`. Έτσι το συγκεντρωτικό PDF αποκτά εξώφυλλο, ενιαία
περιεχόμενα και συνεχή αρίθμηση σελίδων.

## Οδηγίες συγγραφής

- Το κείμενο είναι στα ελληνικά. Κρατάμε το ίδιο ύφος (β' πληθυντικό, φιλικό αλλά
  διδακτικό) και αφήνουμε στα αγγλικά τα ονόματα μεταβλητών, τις εντολές και τις
  λέξεις-κλειδιά της C.
- Τα μαθηματικά γράφονται με `$...$` / `$$...$$` και αποδίδονται από το MathJax στην
  ιστοσελίδα και από το xelatex στο PDF — πρέπει να δουλεύουν και στα δύο.
- Κάθε αρχείο `labs/labNN/README.md` ξεκινά με front matter (`layout`, `lab`,
  `prev`, `next`) που χρησιμοποιεί το Jekyll για την πλοήγηση. Το pandoc το αγνοεί,
  οπότε δεν εμφανίζεται στο PDF.

### Η δομή ενός εργαστηρίου

Όλα τα εργαστήρια έχουν την ίδια σειρά. Το `tools/lint.py` την ελέγχει, οπότε αν
κάτι λείπει ή είναι εκτός σειράς θα το πει το CI:

1. front matter
2. `# Εργαστήριο #N: Τίτλος`
3. το εισαγωγικό πλαίσιο — **Στόχοι**, **Προαπαιτούμενα:**, **Αρχεία που θα
   φτιάξετε:**, πάντα και τα τρία, πάντα με αυτή τη σειρά
4. μία παράγραφος εισαγωγής
5. `<!-- toc -->` … `<!-- /toc -->`
6. οι ενότητες `## Βήμα N:` και μετά οι `## Άσκηση N:`
7. `## Για να πάτε παρακάτω (Προαιρετικό)` — προαιρετικά
8. `## Παράρτημα: …` — πάντα τελευταίο

### Οι τέσσερις ενότητες (H2)

| Είδος | Μορφή | Πότε |
|---|---|---|
| Βήμα | `## Βήμα N: Τίτλος` | καθοδηγούμενο βήμα· δεν παραδίδεται κάτι |
| Άσκηση | `## Άσκηση N: Τίτλος (file.c)` | ο φοιτητής φτιάχνει το `file.c` |
| Παραπέρα | `## Για να πάτε παρακάτω (Προαιρετικό)` | σύνδεσμοι για συνέχεια |
| Παράρτημα | `## Παράρτημα: Τίτλος` | τελευταίο· ένα ανά εργαστήριο |

Το όνομα του αρχείου που πρέπει να παραδώσει ο φοιτητής μπαίνει **πάντα** στον τίτλο
της άσκησης και **πρέπει** να συμφωνεί με τη λίστα «Αρχεία που θα φτιάξετε».

### Σημάνσεις

Μόνο τρεις, με αυτή τη σειρά, χωρισμένες με κόμμα, μετά τον αριθμό και πριν την άνω
κάτω τελεία:

`(Παλιό θέμα)` · `(Προχωρημένο)` · `(Προαιρετικό)`

```text
## Άσκηση 3 (Παλιό θέμα, Προαιρετικό): Σκαλί-Σκαλί (ladder.c)
```

### Υποερωτήματα

- Βήμα-εντολή που ξεκινά με ρήμα και δεν έχει δικό του τίτλο → **έντονο** `**N.M**`
  μέσα στο κείμενο. Δεν μπαίνει στα περιεχόμενα.
- Υποερώτημα με δικό του τίτλο που αξίζει να βρίσκει κανείς → `### N.M Τίτλος`,
  χωρίς τελεία στο τέλος.

Μια **επέκταση** μιας άσκησης δεν γίνεται ποτέ ξεχωριστή ενότητα H2 — μπαίνει ως
`**N.M (Προχωρημένο, Προαιρετικό)**` ή, αν είναι μεγάλη, ως
`### N.M Τίτλος (Προχωρημένο, Προαιρετικό)`.

### Επικεφαλίδες και σύνδεσμοι

Οι επικεφαλίδες είναι ετικέτες, όχι προτάσεις: χωρίς τελεία στο τέλος και κάτω από 80
χαρακτήρες. Αν θέλετε σύντομο σύνδεσμο για μια ενότητα με περιγραφικό τίτλο, βάλτε
ρητή άγκυρα από πάνω της:

```text
<a id="webmail"></a>

## Βήμα 1: Το ακαδημαϊκό σας email (webmail)
```

Έτσι ο σύνδεσμος μένει `#webmail` ακόμα κι αν αλλάξει ο τίτλος.

### Μπλοκ κώδικα

Πάντα με ετικέτα γλώσσας, και μόνο από αυτές: `c`, `text`, `mermaid`, `make`.
Τα terminal transcripts και η έξοδος προγραμμάτων παίρνουν `text` (όχι `sh` ή
`bash` — παλιότερα χρησιμοποιούνταν και τα τρία για το ίδιο πράγμα).

### Παραρτήματα

Τα παραρτήματα αποσφαλμάτωσης είναι **μία σειρά** μέσα στο βιβλίο, όχι πέντε
ανεξάρτητα κείμενα: `## Παράρτημα: Αποσφαλμάτωση προγραμμάτων (Πράξη Nη)`, με το `N`
να τρέχει 1…5 (lab02, lab03, lab07, lab08, lab09). Μια άσκηση που ανήκει σε
παράρτημα μπαίνει **μέσα** του ως `### Άσκηση N: …`, συνεχίζοντας την αρίθμηση του
εργαστηρίου.

> **Προσοχή στο πλάτος της σελίδας.** Ό,τι φαίνεται μια χαρά στο GitHub δεν χωράει
> απαραίτητα στο PDF. Πριν στείλετε αλλαγή που αγγίζει πίνακες ή μπλοκ κώδικα,
> τρέξτε `make build/labNN.pdf` και δείτε το αποτέλεσμα.
